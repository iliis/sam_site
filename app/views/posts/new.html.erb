<h2>post for project '<%= @project.title %>'</h2>

<%= form_for([@project, @post]) do |f| %>
	<%= f.label :title, "Title:" %>
	<%= f.text_field :title %>
	<br>
	<%= f.text_area :content %>
	<br>
	<%= f.submit %>
<% end %>
