<h2>create new project</h2>

<%= form_for @project do |f| %>
	<%= f.label :title, "title:" %>
	<%= f.text_field :title %><br>

	<%= f.text_area :description %>
	<br>

	<%= f.submit "save" %>
<% end %>
